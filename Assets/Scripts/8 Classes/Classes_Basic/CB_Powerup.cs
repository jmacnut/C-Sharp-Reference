using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CB_Powerup : MonoBehaviour
{
   private WeaponStats blasters;

   //void Start()
   //{
   //   // assigns a weapon to give to player upon collecting
   //   blasters = new WeaponStats("Blaster", 0.25f, 50);
   //}

   //void Update()
   //{

   //}

   //private void OnTriggerEnter2D(Collider2D other)
   //{
   //   if ((
   //      // pass object as a parameter
   //      CB_UIManager.Instance != null) && (other.tag == "CB_Player"))
   //   {
   //      // pass object as a parameter
   //      CB_UIManager.Instance.UpdateWeaponStats(blasters);
   //      Destroy(this.gameObject);
   //   }
   //}
}
